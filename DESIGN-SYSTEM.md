# Типографіка — специфікація

Джерело правди — секція `Text styles` у [`assets/css/style.css`](assets/css/style.css).
Цей документ описує ту саму систему в термінах Figma, щоб її можна було зібрати
без вимірювання скріншотів.

---

## 1. Як це влаштовано в CSS

Три шари, саме в такому порядку:

| Шар | Де | Що робить |
|---|---|---|
| Примітиви | `:root` | Сирі значення: сімʼя, насиченість, розмір, інтерліньяж, трекінг |
| Текстові стилі | секція `Text styles` | Композити. Один блок = один стиль у Figma |
| Компоненти | усе нижче | Тільки лейаут і колір. **Жодних шрифтових властивостей** |

Компонент не оголошує метрики — він **приєднується** до стилю тим, що стоїть
у його селекторі. Тому кожне правило одночасно є списком «де цей стиль вживається»:

```css
/* Heading/L — 24→34 · 700 · 105% · -3% */
.t-heading-l,
.work-row__title,
.case-section__title{ … }
```

Правило: селектори по тегу в цьому шарі завжди загорнуті в `:where()`, щоб мати
нульову вагу. Стиль обирається класом, а не глибиною вкладеності.

---

## 2. Плинні розміри → два режими Figma

Розміри інтерполюються лінійно між вьюпортом **375px** і **1440px**:

```
clamp(<значення на 375>, <перетин> + <нахил>vw, <значення на 1440>)
```

Обидва краї — точні числа, не «на око». У Figma це колекція змінних
`Type/Size` з двома режимами:

| Режим | Вьюпорт |
|---|---|
| `Mobile` | 375 |
| `Desktop` | 1440 |

Аналога `clamp()` у Figma немає й не потрібно: вводиш два крайні значення,
проміжні браузер порахує сам.

Драбина Display геометрична, крок ≈1.33: **128 · 96 · 72 · (56) · 44**.
Розміри body — фіксовані: текст для читання не має масштабуватись від вьюпорта.

---

## 3. Примітиви

### Сімʼя

Одна гарнітура — **Inter**. `--font-display` і `--font-body` — це аліаси на неї;
різниця між ними лише в насиченості. У Figma окремої змінної для «display»
робити не треба.

Підключені лише **400 / 500 / 700**. Просити щось поза цим набором не можна —
браузер синтезує фальшиве накреслення. Тому й `<strong>` обмежений до 500.

### Насиченість

| Змінна | Значення | Figma |
|---|---|---|
| `--fw-regular` | 400 | Regular |
| `--fw-medium` | 500 | Medium |
| `--fw-bold` | 700 | Bold |

### Розмір

| Змінна | Mobile (375) | Desktop (1440) |
|---|---|---|
| `--fs-display-2xl` | 44 | 128 |
| `--fs-display-xl` | 38 | 96 |
| `--fs-display-l` | 34 | 72 |
| `--fs-display-ml` | 30 | 56 |
| `--fs-display-m` | 28 | 44 |
| `--fs-heading-l` | 24 | 34 |
| `--fs-heading-m` | 20 | 26 |
| `--fs-heading-s` | 18 | 20 |
| `--fs-body-xl` | 17 | 20 |
| `--fs-body-l` | 17 | 17 |
| `--fs-body-m` | 15 | 15 |
| `--fs-body-s` | 13 | 13 |
| `--fs-label-m` | 12 | 12 |
| `--fs-label-s` | 11 | 11 |
| `--fs-label-xs` | 10 | 10 |
| `--fs-print-title` | 14 | 14 |
| `--fs-print-body` | 11 | 11 |
| `--fs-print-label` | 10 | 10 |

`--fs-display-ml` (30→56) наразі не вживається — тримає драбину регулярною
й закриває розрив між 44 і 72, коли зʼявиться потреба.

### Інтерліньяж

CSS тримає безрозмірні числа, Figma — відсотки. Це те саме число.

| Змінна | CSS | Figma | Для чого |
|---|---|---|---|
| `--lh-display` | 0.92 | 92% | Display, завжди короткі рядки |
| `--lh-flat` | 1 | 100% | Однорядкові лейбли, кнопки, чипи |
| `--lh-tight` | 1.05 | 105% | Display/M і Heading/L |
| `--lh-snug` | 1.2 | 120% | Малі заголовки |
| `--lh-normal` | 1.5 | 150% | UI-текст, списки визначень, ліди |
| `--lh-relaxed` | 1.6 | 160% | Суцільний текст і лейбли, що переносяться |

### Трекінг

Оптичний, привʼязаний до розміру: що більший кегль — то щільніше набір.
`1em` у CSS = `100%` у Figma.

| Змінна | CSS | Figma | Діапазон |
|---|---|---|---|
| `--ls-display` | -0.045em | -4.5% | 56px і більше |
| `--ls-heading` | -0.03em | -3% | 24…44px |
| `--ls-tight` | -0.02em | -2% | 14…26px |
| `--ls-normal` | 0 | 0% | увесь body |
| `--ls-label` | 0.1em | +10% | капсові лейбли |

---

## 4. Текстові стилі

Розмір подано як `Mobile / Desktop`. «Case» — це поле *Letter case* у Figma.

### Display

| Стиль | Вага | Розмір | LH | LS | Case | Де |
|---|---|---|---|---|---|---|
| `Display/2XL` | 700 | 44 / 128 | 92% | -4.5% | Upper | герой головної |
| `Display/XL` | 700 | 38 / 96 | 92% | -4.5% | Upper | герої сторінок, герой кейсу |
| `Display/L` | 700 | 34 / 72 | 92% | -4.5% | As typed | email-адреси, число в stat-card |
| `Display/L Caps` | 700 | 34 / 72 | 92% | -4.5% | **Upper** | про мене, наступний кейс, рішення, 404, мобільне меню |
| `Display/M` | 700 | 28 / 44 | 105% | -3% | As typed | назва проєкту |
| `Display/M Caps` | 700 | 28 / 44 | 105% | -3% | **Upper** | імʼя в резюме |

### Heading

| Стиль | Вага | Розмір | LH | LS | Де |
|---|---|---|---|---|---|
| `Heading/L` | 700 | 24 / 34 | 105% | -3% | рядок роботи, заголовок секції кейсу |
| `Heading/M` | 700 | 20 / 26 | 120% | -2% | розвилка рішення, email на картці |
| `Heading/S` | 700 | 18 / 20 | 120% | -2% | NDA-блок, пункт обмежень |

### Body

| Стиль | Вага | Розмір | LH | LS | Де |
|---|---|---|---|---|---|
| `Body/XL` | 400 | 17 / 20 | 150% | 0 | тільки ліди |
| `Body/L` | 400 | 17 | 160% | 0 | суцільний текст |
| `Body/L Medium` | 500 | 17 | 100% | 0 | самостійні посилання |
| `Body/M` | 400 | 15 | 160% | 0 | другорядний текст |
| `Body/M Medium` | 500 | 15 | 100% | 0 | рядки інлайн-посилань |
| `Body/S` | 400 | 13 | 150% | 0 | щільні рядки, підписи |

### Label

Усі — Upper, трекінг +10%.

| Стиль | Вага | Розмір | LH | Де |
|---|---|---|---|---|
| `Label/M` | 400 | 12 | 160% | статус у герої (переноситься) |
| `Label/M Compact` | 400 | 12 | 100% | шапка, футер, індекс рядка |
| `Label/S` | 400 | 11 | 160% | **основний лейбл** — усі кікери, дескриптори, мета |
| `Label/S Compact` | 400 | 11 | 100% | чипи поверх зображень, підписи на темному |
| `Label/XS` | 400 | 10 | 100% | підпис у мініатюрі |
| `Label/Button` | 500 | 13 | 100% | кнопки (LS 0, Case as typed) |

`Label/S` — робоча конячка системи. У розмітці її іменований вхід — клас
`.mono-label`.

### Print

Резюме — це A4-документ, який просто відкривається в браузері: його колонка
завжди 794px незалежно від вікна. Плинна вебдрабина до нього не застосовується,
тому це окрема маленька родина стилів, а не вигин основної.

| Стиль | Вага | Розмір | LH | LS | Case |
|---|---|---|---|---|---|
| `Print/Title` | 700 | 14 | 120% | -2% | As typed |
| `Print/Body` | 400 | 11 | 160% | 0 | As typed |
| `Print/Label` | 400 | 10 | 160% | +10% | Upper |

---

## 5. Кольори тексту

Текстовий стиль **ніколи не несе заливки** — колір це окремий токен. Тому
будь-який стиль може стояти в будь-якому рівні.

Три рівні на кожному тлі:

| Токен | Hex | Рівень |
|---|---|---|
| `Text/Primary` | `#111112` | заголовки, основний текст |
| `Text/Secondary` | `#443F38` | супровідний текст, ліди |
| `Text/Tertiary` | `#7A776F` | лейбли, мета |
| `Text/Inverse/Primary` | `#F0EEE9` | те саме на темних секціях |
| `Text/Inverse/Secondary` | `#B9B8B3` | |
| `Text/Inverse/Tertiary` | `#8A8A85` | |
| `Text/Accent` | `#2436D8` | акцентні посилання |
| `Print/Ink` | `#000000` | резюме |
| `Print/Secondary` | `#333333` | |
| `Print/Tertiary` | `#555555` | |

---

## 6. Порядок збірки у Figma

1. **Колекція змінних `Type/Size`**, два режими — `Mobile` і `Desktop`.
   Завести 18 змінних із таблиці §3.
2. **Колекція `Type/Primitive`**, один режим: інтерліньяж (число) і трекінг
   (число, у відсотках) із §3.
3. **Кольорові стилі** з §5 — окремо, не всередині текстових.
4. **Текстові стилі** з §4. Розмір привʼязати до змінної з кроку 1 —
   тоді перемикання режиму `Mobile`/`Desktop` на фреймі перебудує весь макет.
5. Іменування — рівно як у таблиці, зі скісною рискою: Figma сама складе
   з `Display/2XL` групу `Display` і стиль `2XL`.

Про `Case`: у Figma регістр входить у текстовий стиль і його не можна
перевизначити на шарі, не відірвавши стиль. Тому там, де роль трапляється і в
капсі, і як набрано, стилів два — `Display/L` та `Display/L Caps` з однаковими
метриками.

---

## 7. Що змінилось при уніфікації

Було **42** різні шрифтові оголошення на **25** ролей. Звідси й розбіжності:

- **19 значень інтерліньяжу** (0.9, 0.92, 0.95, 1, 1.02, 1.05, 1.1, 1.15, 1.2,
  1.3, 1.4, 1.45, 1.5, 1.55, 1.6, 1.65, 1.7, 1.8, 1.9) → **6**
- **9 значень трекінгу** → **5**
- **13 окремих `clamp()`** для заголовків, кожен зі своєю формулою → одна
  драбина з 5 щаблів
- `#443F38` стояв інлайном **9 разів** → токен `Text/Secondary`

Розміри, які помітно зсунулись (усе — на 375px):

| Елемент | Було | Стало |
|---|---|---|
| Заголовок 404 | 48.1 | 34 |
| Про мене / наступний кейс | 41.1 | 34 |
| Число в stat-card | 40.0 | 34 |
| Герої сторінок і кейсу | 41.1–41.7 | 38 |
| Назва проєкту | 25.9 | 28 |

Це і є суть уніфікації: на одну роль було пʼять різних мобільних розмірів
(38, 40, 41.1, 41.7, 48.1). На десктопі зсув менший — драбина будувалась
навколо наявних значень.

### Виправлений баг

`.case-section p` і `.about-tools p` мали вагу `(0,1,1)` і перебивали
`.mono-label` з вагою `(0,1,0)`. Через це кікери `01 контекст`, `03 процес`,
`06 рефлексія` та `інструменти` малювались як 17px основний текст замість 11px
капсового лейбла. Помилка була в коді до рефакторингу. Полагоджено тим, що
селектори по тегу тепер у `:where()`.

### Відомий залишок

`.btn` / `.btn--solid` описані, але в розмітці не вживаються — на них немає
жодного `class="btn"`. Стиль `Label/Button` залишений у системі, бо кнопка
знадобиться; сам CSS-блок можна видалити, якщо ні.
